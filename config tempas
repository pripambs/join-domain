   sudo apt-get install nginx mariadb-server certbot php8.1 php8.1-cli php8.1-mysql php8.1-curl php8.1-mbstring php8.1-bcmath php8.1-common php8.1-fpm php8.1-gd php8.1-xml git wget
   sudo nano /etc/php/8.1/fpm/php.ini
   sudo systemctl restart php8.1-fpm.service
   sudo mysql_secure_installation
   sudo mysql -u root -p
   sudo mkdir -p /var/teampass
   sudo chown -R www-data:www-data /var/teampass
   cd /var/www/html
   sudo git clone https://github.com/nilsteampassnet/TeamPass.git
   cd ~
   sudo mkdir TeamPass/includes/libraries/csrfp/log
   cd /var/www/html
   sudo mkdir TeamPass/includes/libraries/csrfp/log
   cd TeamPass/
   cd ~
   sudo chown -R www-data:www-data /var/www/html/TeamPass/
   sudo chmod -R 775 /var/www/html/TeamPass/
   sudo systemctl stop nginx
   sudo systemctl start nginx
   sudo nano nano /etc/nginx/sites-available/teampass.conf
   sudo nano /etc/nginx/sites-available/teampass.conf
   sudo nano nano /etc/nginx/sites-available/teampass.conf
   rm -f /etc/nginx/sites-available/teampass.conf
   sudo rm -f /etc/nginx/sites-available/teampass.conf
   sudo nano /etc/nginx/sites-available/teampass.conf
   sudo systemctl stop nginx
   sudo certbot certonly -d teampass.diamond.co.id
   sudo certbot certonly -d teampass.diamond.local
   sudo certbot certonly -d teampass.diamond.xyz
   sudo systemctl start nginx
   sudo nano /etc/nginx/sites-available/teampass.conf
   sudo ln -s /etc/nginx/sites-available/teampass.conf /etc/nginx/sites-enabled/teampass.febryandana.conf
   sudo nginx -t
   sudo nano /etc/nginx/sites-available/teampass.conf
   sudo nginx -t
   sudo systemctl restart nginx
   sudo systemctl status nginx
   sudo nano /etc/php/8.1/fpm/php.ini
   sudo systemctl restart php8.1-fpm.service
   sudo apt install php8.1-gmp
   sudo systemctl restart php8.1-fpm.service
   sudo rm -rf /var/www/html/TeamPass/install
